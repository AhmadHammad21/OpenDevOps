{
  "name": "Lambda timeout lowered to 1s — function now always times out",
  "cloud": "aws",
  "demo": "demos/aws/m2_timeout_misconfig.py",
  "prompt": "opendevops-demo-timeout is failing intermittently, why?",
  "timeout_seconds": 240,
  "propagation_seconds": 30,
  "ground_truth": {
    "root_cause_category": ["SYSTEM_CHANGE", "RESOURCE_LIMIT"],
    "services_affected": ["Lambda"],
    "evidence_keywords": ["timed out", "timeout"],
    "expected_tools_any_of": [
      "get_log_events",
      "get_lambda_function_config",
      "lookup_cloudtrail_events"
    ]
  }
}
